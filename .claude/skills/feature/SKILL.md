## Add Feature (TDD)
1. Write failing test first in the appropriate test file
2. Implement the minimal code to pass
3. Run `pnpm test` and verify green
4. If Django-side changes needed, run `python manage.py test`
5. Update any Docker configs if new deps added
