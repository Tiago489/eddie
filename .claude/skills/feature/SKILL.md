## Add Feature (TDD)
1. Write failing test(s) for the requested feature
2. Run `pnpm test` to confirm they fail
3. Implement the minimal code to pass
4. Run `pnpm test` to confirm ALL tests pass (baseline: 211+)
5. Refactor if needed, re-run tests
6. Report: new tests added, total passing count
